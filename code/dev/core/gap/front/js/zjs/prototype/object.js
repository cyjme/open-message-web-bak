//
// use type will cause facebook error: Uncaught Error: EventPluginRegistry: Cannot inject event plugins that do not exist in the plugin ordering, `type`.
//

/*
Object.prototype.ztype = function() {
    return this.toString().slice(8, -1).toLowerCase();
};
*/
