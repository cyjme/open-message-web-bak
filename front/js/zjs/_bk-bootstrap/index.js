import './../prototype';
//import {loader} from './../loader';

//document.ready(() => loader.load());

//import {zon} from './../zon';

/*
zon.on('require', elem => {
    if (zon.requirePage) {
        zon.requirePage(elem.getAttribute('data-page'));
    }
});
*/

/*
zon.reg();

document.ready(() => {
    if (zon.hasEvent('load')) {
        zon.trigger('load');
    }
});
*/
